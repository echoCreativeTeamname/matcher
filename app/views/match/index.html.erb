<h2><i>Er zijn <%= @ingredientCount %> <a href="/match/ingredients">ingrediënt<%= (@ingredientCount != 1) ? "en" : "" %></a> &amp; <%= @recipeCount %> <a href="/match/recipes">recept<%= (@recipeCount != 1) ? "en" : "" %></a> die nog moeten worden gematcht</i></h2>
<p>Recepten die nog niet gematcht zijn worden niet weergeven door de API, van ongematchte ingredienten kan geen prijs worden gegeven.</p>
<br />
<div class="row">
  <div class="col-md-6">
    <h2>Ingrediënten</h2>
    <% if @unmatchedIngredients.size != 0 %>
    <table class="table table-bordered">
      <tr>
        <th style="text-align: center;">#</th>
        <th>naam</th>
      </tr>
      <% @unmatchedIngredients.each do |ingredient| %>
      <tr onclick="window.location='ingredients/<%= ingredient.id %>'">
        <td style="text-align: center;"><%= ingredient.id %></td>
        <td><%= ingredient.name %></td>
      </tr>
      <% end %>
    </table>
    <% else %>
    <p>Er zijn geen ongematchte ingrediënten.</p>
    <% end %>
  </div>
  <div class="col-md-6">
    <h2>Recepten</h2>
    <% if @unmatchedRecipes.size != 0 %>
    <table class="table table-bordered">
      <tr>
        <th style="text-align: center;">#</th>
        <th>naam</th>
      </tr>
      <% @unmatchedRecipes.each do |recipe| %>
      <tr onclick="window.location='recipes/<%= recipe.id %>'">
        <td style="text-align: center;"><%= recipe.id %></td>
        <td><%= recipe.name %></td>
      </tr>
      <% end %>
    </table>
    <% else %>
    <p>Er zijn geen ongematchte recepten.</p>
    <% end %>
  </div>
</div>
